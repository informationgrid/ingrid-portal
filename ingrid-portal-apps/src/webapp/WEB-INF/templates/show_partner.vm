  <p>Wählen Sie einen der folgenden Partner aus. Jedem Partner können mehrere Informationsanbieter zugeordnet sein.</p>
  <ul class="innermenu">
#foreach( $key in $partners.keySet() ) 
	#set($partnerHash = $partners.get($key))
	#set($partner = $partnerHash.partner)
	#if($partnerHash.providers)
	<li><a href="#$partner.ident">$partner.name</a></li>
	#end
#end
  </ul>
  <br />
#foreach( $key in $partners.keySet() ) 
	#set($partnerHash = $partners.get($key))
	#set($partner = $partnerHash.partner)
	#if($partnerHash.providers)
  <a name="$partner.ident"></a>
  <div class="line"><hr /></div>
  <br />
  <h3>$partner.name</h3>
		#set($providers = $partnerHash.providers)
		#foreach($provider in $providers)
			<p>#if($provider.provider.url && $provider.provider.url != "")<a href="$provider.provider.url" target="_new">$provider.provider.name</a>#else$provider.provider.name#end</p>
			<ul>
			#foreach($plug in $provider.iplugs)
				<li>$plug.getDataSourceName()</li>
			#end
			</ul>
		#end
	#end
#end
