<div class="pagination bx-top-m mq-hide-l">
## RANKED (left column stuff)
#if($grouping)
	#if($grouping == 'domain')
		#renderRankedPageNavigationGroupingWithPages($rankedPageSelector true)
	#else
		#renderRankedPageNavigationGrouping($rankedPageSelector true)
	#end
#else
	#renderRankedPageNavigation($rankedPageSelector true)
#end
</div>