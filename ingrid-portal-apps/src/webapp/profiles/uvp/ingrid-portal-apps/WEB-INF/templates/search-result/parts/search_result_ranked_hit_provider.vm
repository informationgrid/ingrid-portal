#set ($showProvider = true)
#set ($uvp_address = "")
#set ($uvp_steps = false)

#if($grouping && $grouping == 'provider')
    #set ($showProvider = false)
#end
#if($showProvider)
    #if($rs.get("detail"))
        #set ($uvp_address = $rs.get('detail').get('uvp_address'))
        #if($uvp_address && $uvp_address != "")
            <h6 class="no-margin">$MESSAGES.getString('common.result.provider')</h6>
            <p>
            #foreach($address in $uvp_address)
                <small>$address</small><br>
            #end
            </p>
        #end
        #set($uvp_steps = $UTIL_SEARCH.getFilteredDuplicateDetailValue($rs.get('detail'), 'uvp_steps'))
        #if($uvp_steps)
            #set($steps = $uvp_steps.split(","))
            #if($steps && $steps.size() > 0)
                <h6 class="no-margin">Letzter Verfahrensschritt</h6>
                <p>
                    #set($lastEntry = $steps.size() - 1)
                    <small>$MESSAGES.getString("common.steps.uvp.$steps.get($lastEntry).trim()")</small>
                </p>
            #end
        #end
    #end
#end