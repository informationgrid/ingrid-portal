#if($mapImage && $mapImage.elements && $mapImage.elements.size() > 0)
    <div class="xsmall-24 small-24 medium-10 columns">
        <h5 class="text-center">$MESSAGES.getString("preview")</h5>
        #renderMultiLineImage($mapImage)
        #set($mapImage = "")
    </div>
#end