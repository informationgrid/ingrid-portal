## Verweise und Downloads
################################
#if($hasDetail2)
    <a class="anchor" id="${hashDetail2}" name="${hashDetail2}"></a>
    <h2>$MESSAGES.getString("references")</h2>
    <div class="row columns">
    ## Links
        #if($getLinks.size() > 0)
            #set($tmpTitle = $MESSAGES.getString("search.detail.dataRelations"))
            #set($tmpCount = $getLinks.size())
            #if($tmpCount)
                #set($tmpTitle = $tmpTitle + " <span>(${tmpCount})</span>")
            #end
            <a class="anchor" id="${hashDetail2}_info_links" name="${hashDetail2}_info_links"></a>
            #renderLinkListImageLimit($tmpTitle $getLinks false true $getLinks.size() "links_more" "h3" [] true)
        #end
    ## Übergeordnete Adressen
        #if($getSuperiorLinks.size() > 0)
            #set($tmpTitle = $MESSAGES.getString("superior_address"))
            #set($tmpCount = $getSuperiorLinks.size())
            #if($tmpCount)
                #set($tmpTitle = $tmpTitle + " <span>(${tmpCount})</span>")
            #end
            <a class="anchor" id="${hashDetail2}_superior_address" name="${hashDetail2}_superior_address"></a>
            #renderLinkListImageLimit($tmpTitle $getSuperiorLinks false true $getSuperiorLinks.size() "superior_more" "h3" [] true)
        #end
    ## Untergeordnete Adressen
        #if($getSubordinatedLinks.size() > 0)
            #set($tmpTitle = $MESSAGES.getString("subordinated_addresses"))
            #set($tmpCount = $getSubordinatedLinks.size())
            #if($tmpCount)
                #set($tmpTitle = $tmpTitle + " <span>(${tmpCount})</span>")
            #end
            <a class="anchor" id="${hashDetail2}_subordinated_addresses" name="${hashDetail2}_subordinated_addresses"></a>
            #renderLinkListImageLimit($tmpTitle $getSubordinatedLinks false true $getSubordinatedLinks.size() "subordinated_more" "h3" [] true)
        #end
    </div>
#end