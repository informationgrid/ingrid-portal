    ## Identifikator der Datenquelle
        #renderTextTable("Identifikator" $TOOL.getValueFromXPath($xPathInfo1MDIdentifierCode))
    ## Identifikator der Datenquelle (older iplug)
        #renderTextTable("Identifikator" $TOOL.getValueFromXPath($xPathInfo1RSIdentifierCode))