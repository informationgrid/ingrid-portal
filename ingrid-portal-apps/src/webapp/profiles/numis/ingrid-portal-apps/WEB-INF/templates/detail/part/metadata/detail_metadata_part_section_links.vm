## Verweise und Downloads
################################
#if($hasDetail3)
    <a class="anchor" id="detail-3" name="detail-3"></a>
    <h2>$MESSAGES.getString("references_tab")</h2>
    <div class="row columns">
    ## Downloads
        #if($getDownloads.size() > 0)
            #set($tmpTitle = $MESSAGES.getString("downloads"))
            #set($tmpCount = $getDownloads.size())
            #if($tmpCount)
                #set($tmpTitle = $tmpTitle + " <span>(${tmpCount})</span>")
            #end
            #renderLinkListImageLimit($tmpTitle $getDownloads false false $getDownloads.size() "downloads_more" "h3")
        #end
    ## Links
        #if($getLinks.size() > 0)
            #set($tmpTitle = $MESSAGES.getString('search.detail.info.links'))
            #set($tmpCount = $getLinks.size())
            #if($tmpCount)
                #set($tmpTitle = $tmpTitle + " <span>(${tmpCount})</span>")
            #end
            #renderLinkListImageLimit($tmpTitle $getLinks false true $getLinks.size() "links_more" "h3" [] true)
        #end
    ## Weitere Verweise
        #if($getOtherLinks.size() > 0)
            #set($tmpTitle = $MESSAGES.getString("www_references"))
            #set($tmpCount = $getOtherLinks.size())
            #if($tmpCount)
                #set($tmpTitle = $tmpTitle + " <span>(${tmpCount})</span>")
            #end
            #renderLinkListImageLimit($tmpTitle $getOtherLinks false false $getOtherLinks.size() "others_more" "h3")
        #end
        
    ## Untergeordnete Objekte
        #if($getSubordinatedLinks.size() > 0)
            #set($tmpTitle = $MESSAGES.getString("subordinated_references"))
            #set($tmpCount = $getSubordinatedLinks.size())
            #if($tmpCount)
                #set($tmpTitle = $tmpTitle + " <span>(${tmpCount})</span>")
            #end
            #renderLinkListImageLimit($tmpTitle $getSubordinatedLinks false true $getSubordinatedLinks.size() "subordinated_more" "h3" [] true)
        #end
    ## Übergeordnetet Objekte
        #if($getSuperiorLinks.size() > 0)
            #set($tmpTitle = $MESSAGES.getString("superior_references"))
            #set($tmpCount = $getSuperiorLinks.size())
            #if($tmpCount)
                #set($tmpTitle = $tmpTitle + " <span>(${tmpCount})</span>")
            #end
            #renderLinkListImageLimit($tmpTitle $getSuperiorLinks false true $getSuperiorLinks.size() "superior_more" "h3" [] true)
        #end
    </div>
#end