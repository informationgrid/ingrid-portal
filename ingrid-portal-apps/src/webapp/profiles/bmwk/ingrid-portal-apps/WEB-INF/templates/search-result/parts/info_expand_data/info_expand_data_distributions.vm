## Ressourcen
#set($distribution_title = [])
#if($hit.getHitDetail().get("distribution.title"))
    #set($distribution_title = $hit.getHitDetail().get("distribution.title"))
#end
#set($distribution_description = [])
#if($hit.getHitDetail().get("distribution.description"))
    #set($distribution_description = $hit.getHitDetail().get("distribution.description"))
#end
#set($distribution_access_url = [])
#if($hit.getHitDetail().get("distribution.access_url"))
    #set($distribution_access_url = $hit.getHitDetail().get("distribution.access_url"))
#end
