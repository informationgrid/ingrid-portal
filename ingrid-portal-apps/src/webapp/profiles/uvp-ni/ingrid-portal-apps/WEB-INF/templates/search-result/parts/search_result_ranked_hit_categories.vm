#if($rs.get("detail"))
    #set($uvp_category = "$UTIL_SEARCH.getDetailValue($rs.get('detail'), 'uvp_category')")
    
    #if($uvp_category && $uvp_category.length() > 0)
        #set($uvp_categories = $uvp_category.split(","))
        <ul class="tag-group">
        #set($categories = [])
        #foreach($category in $uvp_categories)
            #if($categories.indexOf($category) < 0)
                #set($tmp = $categories.add($category.trim()))
            #end
        #end
        #foreach($category in $categories)
            <li class="tag tag--small">$MESSAGES.getString("searchResult.categories.uvp.$category")</li>
        #end
        </ul>
    #end
    
    #set($procedure = "$UTIL_SEARCH.getDetailValue($rs.get('detail'), 'procedure')")
    #if($procedure)
        #if($procedure == "dev_plan")
            <ul class="tag-group">
                <li class="tag tag--small">$MESSAGES.getString("searchResult.procedure.uvp.$procedure")</li>
            </ul>
        #end
    #end
#end