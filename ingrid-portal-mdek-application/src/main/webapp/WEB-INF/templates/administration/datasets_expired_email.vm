#set($EXP_DS_LIST = $map.get("expiredDatasetList"))
#set($DIRECT_LINK = $map.get("directLink"))
Dies ist eine Meldung von der Metadatenerfassungskomponente der Ingrid Software.
Die Verfallszeitspanne folgender Datensätze ist abgelaufen: 

#foreach($expDS in $EXP_DS_LIST)
Titel: $expDS.title
Uuid: $expDS.uuid
Typ: $expDS.type
Letzte Änderung: $expDS.lastModified
#if( $expDS.type.toString() == "OBJECT")
Direkter Link: ${DIRECT_LINK}%3FnodeType=O%26nodeId=$expDS.uuid
#else
Direkter Link: ${DIRECT_LINK}%3FnodeType=A%26nodeId=$expDS.uuid
#end

#end
