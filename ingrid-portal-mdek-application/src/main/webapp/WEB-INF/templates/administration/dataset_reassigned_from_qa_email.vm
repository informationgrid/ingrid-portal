#set($reassignedDS = $map.get("reassignedDataset"))
Dies ist eine Meldung von der Metadatenerfassungskomponente der Ingrid Software.
Der folgende Datensatz wurde Ihnen von der Qualitätssicherung rücküberwiesen: 

Titel: $reassignedDS.title
Uuid: $reassignedDS.uuid
#if( $reassignedDS.type == "O")
Direkter Link: http://localhost:8080/ingrid-portal/portal/service-myportal.psml?r=http://localhost:8080/ingrid-portal/portal/mdek%3FnodeType=O%26nodeId=$reassignedDS.uuid
#else
Direkter Link: http://localhost:8080/ingrid-portal/portal/service-myportal.psml?r=http://localhost:8080/ingrid-portal/portal/mdek%3FnodeType=A%26nodeId=$reassignedDS.uuid
#end

